# Runtime dependencies — what the app itself needs to serve a game.
# Development extras (tests, asset generation): requirements-dev.txt
# Production server (gunicorn + gevent):        requirements-prod.txt

flask==3.0.3
werkzeug==3.0.2
jinja2==3.1.3
itsdangerous==2.1.2
click==8.1.7

# realtime
flask-socketio==5.4.1
python-socketio==5.11.4
simple-websocket==1.1.0
